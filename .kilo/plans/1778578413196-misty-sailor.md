The `ForgotPasswordComponent` currently implements a multi-step form within a single component (Email -> Code -> New Password) by changing a `step` variable. When the code is successfully verified, the `step` variable is set to `3`, which displays the 'New Password' form within the same component.

To address the issue of 'not redirecting to reset password', I need to clarify your expectation:

1.  **Do you expect a full page navigation to a *new route* (e.g., `/reset-password`) after confirming the code?** This would involve creating a separate component and a new route for the reset password functionality.
2.  **Or, is the current multi-step form approach within the `ForgotPasswordComponent` acceptable, but the UI is not correctly transitioning to 'Step 3: New Password' after you enter the code?**

Please let me know which of these scenarios you are expecting.