# Fix TypeScript Error in UploadImageComponent

The user reported a TypeScript error in `src/app/Components/upload-image-component/upload-image-component.ts` where `this.errorMessage` (which is `string | null`) is passed to `this.toastr.error()`, which expects a `string`.

## Changes
- Modify `src/app/Components/upload-image-component/upload-image-component.ts` to ensure a string is passed to `this.toastr.error()`.

## Implementation Details
- In the `error` callback of `analyzeImage()`, change `this.toastr.error(this.errorMessage)` to `this.toastr.error(this.errorMessage || 'An error occurred')` or use a non-null assertion since the value is assigned just before.
